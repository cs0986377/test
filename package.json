{
  "name": "nodejs",
  "version": "1.0.0",
  "description": "A project in node",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "lite-server",
    "start": " parcel index.html",
    "build": "parcel build index.html",
    "clean": "rimraf dist",
    "Build": "npm run clean && npm run parcel build *.html --public-url ./"
  },
  "author": "Caleb",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.8",
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "lite-server": "^1.3.4",
    "parcel": "^2.12.0",
    "rimraf": "^5.0.5"
  }
}
